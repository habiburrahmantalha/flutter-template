
enum NotificationType{
  notification, profile, home
}

enum NotificationState{
  terminated, foreground, background
}

enum LoadingType{
  otp, login,logout, faq
}

enum ErrorType{
  authentication, system
}


